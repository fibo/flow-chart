{
  "name": "flow-chart",
  "version": "0.0.1",
  "description": "is a collection of React components to draw an algorithm, workflow or process diagram",
  "main": "FlowChart.js",
  "scripts": {
    "babel": "babel src -d .",
    "lint": "cd src; standard; cd --",
    "postbabel": "git add FlowChart.js components/*js; git commit -m 'transpiled'",
    "postversion": "git push origin v${npm_package_version}; npm publish; git push origin master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "lint"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fibo/flow-chart.git"
  },
  "keywords": [
    "flowchart",
    "flow-chart",
    "react",
    "components"
  ],
  "author": "Gianluca Casati (http://g14n.info)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fibo/flow-chart/issues"
  },
  "homepage": "http://g14n.info/flow-chart",
  "peerDependencies": {
    "react": "15.x",
    "react-dom": "15.x"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "pre-commit": "^1.2.2",
    "zeroconf-redux": "^1.2.0",
    "standard": "^10.0.2"
  }
}
